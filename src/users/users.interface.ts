export interface IUser {
  _id: string;
  name: string;
  email: string;
  role?: string;
  // password?: string;
  // age?: string;
  // gender?: string;
  // address?: string;
  // company?: object;
  // refreshToken?: string;
}
